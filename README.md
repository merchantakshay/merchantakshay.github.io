# Akshay Merchant — Personal Website

An editorial personal website covering work, places, interests and social links.

The production build is published from the repository root for GitHub Pages. Editable source lives in `site-src/`.

```bash
cd site-src
npm install
npm run dev
```

Use `npm run build` to create a fresh production build.
